# OpticWitness — deployment configuration (root)
# Copy to .env and fill in. NEVER commit .env or private keys to git.
#
#   cp .env.example .env
#

# Funded StudioNet account private key used to deploy the contract
ACCOUNT_PRIVATE_KEY=

# Target network fee for requests in Wei (Optional, default 0)
FEE_WEI=0
